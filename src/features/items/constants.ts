/**
 * Tamaño de descarga optimizado para imágenes de items
 * Cubre tanto mobile (190px) como desktop (226px)
 */
export const ITEM_IMAGE_SIZE = "(max-width: 768px) 50vw, 190px";

/**
 * Quality para imágenes de items (balance calidad/peso)
 */
export const ITEM_IMAGE_QUALITY = 65;
